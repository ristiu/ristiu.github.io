# Agent instructions

This repo's actual source-of-truth instructions live in [CLAUDE.md](CLAUDE.md) — read it in full before doing anything here, regardless of which agent/tool you are.

For any visual or interaction change, also read the relevant file in [design-direction/](design-direction/) (start with `00-shared-foundations.md`) before writing CSS/JS — it documents how each page is actually built, not just what it looks like, so changes stay consistent with the existing design instead of drifting from it.
